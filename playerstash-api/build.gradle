plugins {
    id 'dev.math3w.playerstash-conventions'
}

publishing {
    publications {
        mainPublication(MavenPublication) {
            artifact tasks.jar
            artifactId 'playerstash-api'
        }
    }
}
